export interface GameType {
	name: string;
	slug: string;
	src: string;
}

export interface ProjectType {
	name: string;
	slug: string;
	description: string;
	githubName: string;
	devpostLink: string;
	projectLink: string;
	image: string;
}
